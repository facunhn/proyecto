# Si dejás esto vacío, la app usa datos mock locales (login, signup y promociones simulados).
# Cuando tengas un backend, poné acá su URL base y se van a usar los endpoints reales:
# GET /promos, POST /promos, POST /auth/login, POST /auth/signup.
VITE_API_BASE_URL=
